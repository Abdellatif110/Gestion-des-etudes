# TODO: Fix Errors, Improve Login/Signup, and Add Project Showcase

## Tasks
- [ ] Fix lint error in AuthForm.tsx (line 43: type the error properly)
- [ ] Improve AuthForm UI: Replace inline styles with Tailwind classes and UI components (Button, Card, Input, etc.)
- [ ] Create authentication store using Zustand for auth state management
- [ ] Integrate React Router in App.tsx: Conditionally render AuthForm first if not authenticated, else main app
- [ ] Add mock authentication logic in AuthForm (simulate login/signup success)
- [ ] Add showcase section/button in main app (e.g., highlight project features like Timer, Tasks)
- [ ] Run lint to confirm no errors
- [ ] Test the app flow (npm run dev)
